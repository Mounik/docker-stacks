# IT-Tools — Useful Utilities

Web-based toolbox with ~100 devops and sysadmin utilities: base64, hash generators, cron parser, regex tester, QR code, JSON formatter, JWT decoder, and more.

## Setup

1. Copy `.env.example` to `.env`
2. Deploy: `make up STACK=it-tools`
3. Use via `ITTOOLS_DOMAIN`
