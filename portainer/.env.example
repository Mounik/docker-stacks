# Portainer
PORTAINER_DOMAIN=portainer.yourdomain.com

# Generate hashed password:
# docker run --rm httpd:alpine htpasswd -nbB admin "YOUR_PASSWORD" | cut -d ':' -f 2
PORTAINER_HASHED_PASSWORD=$2y$05$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx