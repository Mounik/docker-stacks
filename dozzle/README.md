# Dozzle — Real-time Docker Logs Viewer

Web UI to stream and search Docker container logs in real time. Lightweight, no storage needed.

## Setup

1. Copy `.env.example` to `.env`
2. Deploy: `make up STACK=dozzle`

## Usage

Open `DOZZLE_DOMAIN`, log in, and click any container to tail its logs.
