# Vaultwarden
VW_DOMAIN=vault.yourdomain.com
VW_ADMIN_TOKEN=$(openssl rand -base64 48)

# SMTP (for invitations & 2FA)
SMTP_HOST=smtp.gmail.com
SMTP_FROM=your@email.com
SMTP_USER=your@email.com
SMTP_PASSWORD=your-app-password